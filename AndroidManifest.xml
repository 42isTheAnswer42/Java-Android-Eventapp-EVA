<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.eventapp.EVA"
    android:versionCode="11"
    android:versionName="1.12" >

    <uses-permission android:name="android.permission.WAKE_LOCK" >
    </uses-permission>
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.hardware.microphone" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:icon="@drawable/logo2"
        android:label="@string/app_name"
         >
         
        <!--android:theme="@android:style/Theme.Holo.NoActionBar.Fullscreen"-->
        <activity
            android:name="de.eventapp.EVA.start"
            android:excludeFromRecents="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.eventapp.EVA.waver"
            android:excludeFromRecents="true"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="de.eventapp.EVA.maler"
            android:excludeFromRecents="true"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="de.eventapp.EVA.BeatBlitz"
            android:excludeFromRecents="true"
            android:label="@string/app_name" >
        </activity>
    </application>

    <uses-sdk android:minSdkVersion="10" />

</manifest>